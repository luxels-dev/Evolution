name: EvolutionPlugin
version: '${version}'

main: com.helydra.evolutionPlugin.EvolutionPlugin
api-version: '26.1.2'
load: STARTUP

authors: [ Luxels ]

commands:
  skill:
    description: Open the skill interface
  spell:
    description: Open the spell interface
  customgive:
    permission: op